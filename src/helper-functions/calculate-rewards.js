export const calculateRewards = (transactions) => {
  //separate transactions into months
  //calculate rewards for each month
  //calculate total rewards for all months
  //return an object with the monthly rewards and total rewards
};
